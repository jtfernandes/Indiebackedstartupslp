# Buttondown account API key. Get one at https://buttondown.com/settings/api
BUTTONDOWN_API_KEY=

# Comma-separated list of allowed CORS origins. Use the public URL of your
# frontend in production. The dev origin (http://localhost:5173) is always
# allowed regardless of this value.
ALLOWED_ORIGINS=
